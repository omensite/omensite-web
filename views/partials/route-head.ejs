<header class="route-head">
  <div>
    <div class="route-uri">omensite://<%= page.route.uri %></div>
    <div class="route-title"><%= page.route.title %></div>
    <p class="route-desc"><%= page.route.description %></p>
  </div>
  <output class="toast" hidden data-toast></output>
</header>
